const env = process.env.NODE_ENV;

const config = {
  // apiUrl: env === 'development' ? 'http://localhost:5173' : 'https://clownfish-app-4c6ye.ondigitalocean.app',
  apiUrl: 'https://clownfish-app-4c6ye.ondigitalocean.app',
};

export default config;
